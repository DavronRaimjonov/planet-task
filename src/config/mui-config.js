import { createTheme } from "@mui/system";

export const theme = createTheme({
  typography: {
    h1: {
      fontFamily: "Nunito, sans-serif;",
      fontStyle: "normal",
      fontWeight: 700,
      fontSize: "50px",
      color: "#1A1A1A",
    },
  },
});
